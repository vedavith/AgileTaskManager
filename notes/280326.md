# Work Summary - March 27, 2026

## What Confused me?
- Adding the safeUpdate logic took too much time as I didn't understood on How to update it.

## What Broke?
- Whitelisting logic - When I tried to merge {...userData, ...safeUpdate}, I set the safe update object as
`let safeUpdate:any = {email:undefined, name: undefined, password:undefined}`, my idea is to allow single field update as well. I had to Change the logic as `let safeUpdate:any = {}` and later validate what ever fields that are in the safeUpdate object against the UserModel

## Changes Committed (Patch for Users with safeUpdate)
- **User.Model.ts**: Added `updatedAt` field (optional string) to track when users are updated
- **User.Repository.ts**: 
  - Updated `update()` method signature to accept `safeUpdate: Partial<UserModel>` instead of generic `any`
  - Implemented proper merge logic with error handling (returns null if user not found)
  - Added `updatedAt` timestamp on every update
  - Added logging to track existing user, safe update, and result
- **User.Route.ts**: Changed HTTP method from PUT to PATCH for updateUser (semantically correct for partial updates)
- **User.Service.ts**:
  - Modified `validateUser()` to accept `isUpdate` parameter to conditionally validate fields
  - Updated `updateUser()` to build safeUpdate object by whitelisting only name, email, password fields
  - Added duplicate email check before applying update
  - Validation now allows partial updates (fields that aren't provided are not validated)