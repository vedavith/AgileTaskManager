- How did you handle `this` in your class-based controller?
 -- I have declared that variable in the class and in constructor I have assigned value to it.
- Did you feel any confusion while wiring controller → route?
-- Yes,  I was getting these errors.
    ```
        Waiting for the debugger to disconnect...
        /home/ravulave/workspace/AgileTaskManager/node_modules/express/lib/router/index.js:469
        throw new TypeError('Router.use() requires a middleware function but got a ' + gettype(fn))
        ^TypeError: Router.use() requires a middleware function but got a undefined
        at router.use (/home/ravulave/workspace/AgileTaskManager/node_modules/express/lib/router/index.js:469:13)
        at app.<anonymous> (/home/ravulave/workspace/AgileTaskManager/node_modules/express/lib/application.js:227:21)
        at Array.forEach (<anonymous>)
        at app.use (/home/ravulave/workspace/AgileTaskManager/node_modules/express/lib/application.js:224:7)
        at App.mountRoutes (/home/ravulave/workspace/AgileTaskManager/dist/App.js:14:22)
        at new App (/home/ravulave/workspace/AgileTaskManager/dist/App.js:11:14)
        at Object.<anonymous> (/home/ravulave/workspace/AgileTaskManager/dist/App.js:17:19)
        at Module._compile (node:internal/modules/cjs/loader:1829:14)
        at Object..js (node:internal/modules/cjs/loader:1969:10)
        at Module.load (node:internal/modules/cjs/loader:1552:32)
    ```
    -- The problem was with Health.Route.ts which was receiving the undefined instead of the middleware function. 
-- The second issue that I found was, when ever I was hitting `/health` it was returning 404
    -- App.ts Routes was mounting on `health` and Health.Route.ts was also mounting on `/health`. I have changed the inner route to “/” so that the end point becomes health.
- Does your controller contain ONLY HTTP logic?
--Yes, It contains only health check logic that returns the response as expected  


# Day 1 Feedback — Health Endpoint & Initial Architecture

## ✅ What Went Well

### 1. Understanding of `/health` Endpoint
- Identified that the endpoint is used for monitoring and uptime checks
- Moving beyond just “building an endpoint” → starting to think about usage

### 2. Reflection Practice
- Captured learnings instead of just coding
- This is critical for improving engineering depth

---

## ⚠️ Areas to Improve

### 1. Deepen Architecture Thinking

Current thinking:
> Health endpoint is used to check if the app is running

Improved thinking:
- Who calls this endpoint? (Load balancer, Kubernetes, monitoring tools)
- How frequently is it called?
- What happens if it fails?
- Should it include dependency checks?

👉 Key Insight:
Health endpoints are used by infrastructure and must be **fast, lightweight, and reliable**

---

### 2. Add Failure Thinking

Missing considerations:
- What happens if the database is down?
- Should `/health` return OK even if dependencies fail?
- Should we separate:
  - `/health` → basic liveness
  - `/readiness` → dependency health

👉 Action:
Think in terms of **failure scenarios**, not just happy path

---

### 3. Improve `this` Handling Explanation

Current:
> Used arrow functions / handled `this`

Improved:
- Clearly explain *why* the approach was chosen

Example reasoning:
> Arrow functions preserve lexical `this` and avoid binding issues when passing methods as route handlers

Action:
Always justify technical decisions, not just implementation

---

### 4. Clarify Controller Responsibility

Missing clarity:
- Controller responsibility should be explicitly defined

Correct principle:
> Controllers handle HTTP layer (request/response) and should NOT contain business logic

Action:
Add this explicitly to notes and follow it in future implementations

---

### Production Considerations

- Types of health checks:
  - Liveness (is app running?)
  - Readiness (are dependencies ready?)

- Usage:
  - Load balancers use this for routing traffic
  - Monitoring tools use this for alerting

- Design requirements:
  - Must be fast (low latency)
  - Must be lightweight (no heavy operations)

- Failure scenarios:
  - Dependency failure (DB down)
  - Partial system availability
  - Impact on traffic routing

---

## 💡 Key Mindset Shift

Current:
> Building an endpoint

Target:
> Designing a contract used by infrastructure systems

---

## 📌 Action Items for Day 2

- [ ] Update notes with deeper production-level thinking
- [ ] Add failure scenarios to architecture notes
- [ ] Clearly define controller responsibility
- [ ] Improve explanation of `this` handling
- [ ] Refine `/health` endpoint (optional improvements like rounding uptime)

---